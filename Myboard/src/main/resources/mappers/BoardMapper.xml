<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myboard.mapper.BoardMapper">
	
	<sql id="boardColumns">
		idx,
		title,
		content,
		writer,
		view_cnt,
		notice_yn,
		secret_yn,
		delete_yn,
		insert_time,
		update_time,
		delete_time
	</sql>
	
	<insert id="insertBoard" parameterType="BoardDTO">
		INSERT INTO tb_myboard(
			<include refid="boardColumns" />
		) VALUES (
			#{idx},
			#{title},
			#{content},
			#{writer},
			0,
			IFNULL(#{noticeYn} , 'N'),
			IFNULL(#{secretYn} , 'N'),
			'N',
			NOW(),
			NULL,
			NULL
		)
	</insert>
	
	<select id="detailBoard" resultType="BoardDTO" parameterType="long">
		SELECT
			<include refid="boardColumns" />
		FROM
			tb_myboard	
		WHERE
			idx = #{idx}
		AND
			delete_yn = 'N'		
	</select>
	
	<update id="updateBoard" parameterType="BoardDTO">
		UPDATE
			tb_myboard
		SET
			update_time = NOW(),
			title = #{title},
			content = #{content},
			writer = #{writer}
		WHERE
			idx = #{idx}		
	</update>
	
	<update id="deleteBoard" parameterType="long">
		UPDATE
			tb_myboard
		SET
			delete_yn = 'Y',
			delete_time = NOW()
		WHERE
			idx = #{idx}		
	
	</update>
	
	<select id="listBoard" parameterType="BoardDTO" resultType="BoardDTO">
		SELECT
			<include refid="boardColumns" />
		FROM
			tb_myboard
		WHERE
			delete_yn = 'N'
		ORDER BY
			notice_yn ASC,
			idx DESC,
			insert_time DESC			
	</select>
	
	<select id="totalBoardCount" parameterType="BoardDTO" resultType="int">
		SELECT
			count(*)
		FROM
			tb_myboard
		WHERE
			delete_yn = 'N'		
	</select>
	
	<update id="plusView" parameterType="long">
		UPDATE
			tb_myboard
		SET
			view_cnt = view_cnt + 1
		WHERE
			idx = #{idx}		
	</update>
</mapper>